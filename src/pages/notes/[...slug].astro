---
import { getCollection } from 'astro:content';
import Base from '../../layouts/Base.astro';
import Webmentions from '../../components/Webmentions.astro';

export async function getStaticPaths() {
  const notes = await getCollection('notes', ({ data }) => !data.draft);
  return notes.map((note) => ({
    params: { slug: note.slug },
    props: { note },
  }));
}

const { note } = Astro.props;
const { Content } = await note.render();

const formattedDate = new Intl.DateTimeFormat('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
}).format(note.data.date);

const canonical = new URL(`/notes/${note.slug}`, Astro.site ?? 'https://ronbronson.com').toString();
const pageTitle = note.data.title ? `${note.data.title} — Ron Bronson` : 'Note — Ron Bronson';
---

<Base title={pageTitle}>
  <main id="main">
    <article class="h-entry note">
      <header class="note__header">
        <p class="note__kicker">
          <a href="/notes">Notes</a>
        </p>

        <a class="u-url" href={canonical}>
          <time class="dt-published note__date" datetime={note.data.date.toISOString()}>
            {formattedDate}
          </time>
        </a>

        {note.data.title && (
          <h1 class="p-name note__title">{note.data.title}</h1>
        )}

        {note.data.reply_to && (
          <p class="note__reply-to">
            In reply to
            <a class="u-in-reply-to" href={note.data.reply_to}>
              {note.data.reply_to}
            </a>
          </p>
        )}

        <a class="p-author h-card note__author" href="https://ronbronson.com/" rel="author">
          Ron Bronson
        </a>
      </header>

      <div class="e-content note__body">
        <Content />
      </div>

      {note.data.tags.length > 0 && (
        <ul class="note__tags">
          {note.data.tags.map((tag) => (
            <li><span class="p-category">#{tag}</span></li>
          ))}
        </ul>
      )}
    </article>

    <Webmentions url={`/notes/${note.slug}/`} />
  </main>
</Base>

<style>
  .note {
    max-width: var(--measure-narrow);
    margin-inline: auto;
    padding-block: var(--size-10);
    padding-inline: var(--pad);
  }

  .note__header {
    margin-block-end: var(--size-6);
  }

  .note__kicker {
    font-family: var(--font-mono);
    font-size: 0.63rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--ink-faint);
    margin: 0 0 var(--size-3);
  }

  .note__kicker a {
    color: inherit;
    text-decoration: none;
  }

  .note__kicker a:hover {
    color: var(--accent);
  }

  .note__date {
    display: block;
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--ink-mid);
    margin-block-end: var(--size-3);
  }

  .note__title {
    font-family: var(--font-serif);
    font-size: var(--font-size-h1);
    line-height: var(--line-height-h1);
    letter-spacing: -0.02em;
    margin: 0 0 var(--size-3);
  }

  .note__reply-to {
    font-size: 0.9rem;
    color: var(--ink-mid);
    margin-block: var(--size-3);
  }

  .note__reply-to a {
    color: var(--accent);
    word-break: break-all;
  }

  .note__author {
    display: none;
  }

  .note__body {
    font-size: var(--font-size-body);
    line-height: 1.65;
  }

  .note__body :global(p) {
    margin-block: var(--size-4);
    max-width: var(--measure-narrow);
  }

  .note__body :global(a) {
    color: var(--accent);
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: 2px;
  }

  .note__body :global(blockquote) {
    margin-block: var(--size-5);
    margin-inline: 0;
    padding-inline-start: var(--size-4);
    border-inline-start: 2px solid var(--rule);
    color: var(--ink-mid);
    font-style: italic;
  }

  .note__tags {
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    gap: var(--size-2);
    margin: var(--size-6) 0 0;
    padding: 0;
    font-family: var(--font-mono);
    font-size: 0.72rem;
    letter-spacing: 0.04em;
    color: var(--ink-faint);
  }
</style>
